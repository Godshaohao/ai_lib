# Templates

Reusable report or handoff templates can live here.

Current `lib_guard` report source of truth is Python renderer code under `src/lib_guard/render/`. Move a renderer into `templates/` only when the template boundary becomes stable and reduces maintenance cost.
