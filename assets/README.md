# Assets

Static assets for workflow experiments can live here.

For `lib_guard` production HTML, prefer keeping CSS/JS embedded or generated from `src/lib_guard/render/` unless a shared asset becomes stable enough to version explicitly.

Generated report assets under `work/` or `reports/` should not be committed.
